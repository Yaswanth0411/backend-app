import express from "express";
const productRouter = express.Router();

// Add your product routes here
// Example: productRouter.get('/', (req, res) => { res.send('Products'); });

export { productRouter };
